# Review package Task 2
## Diff web/src/lib/sse.ts
```diff
diff --git a/web/src/lib/sse.ts b/web/src/lib/sse.ts index a4f63be..871c4ee 100644 --- a/web/src/lib/sse.ts +++ b/web/src/lib/sse.ts @@ -1,15 +1,52 @@  /**   * 娑堣垂 POST SSE锛坢essages/send 路 card-action锛夈€?  * @author 璧垫尟鏄?- * @date 2026-07-22 08:47:00 + * @date 2026-07-30 12:09:46   */    export type SseHandler = (event: string, data: Record<string, unknown>) => void;   +/** + * 灏?API 閿欒鍝嶅簲瑙ｆ瀽涓哄彲璇讳腑鏂囨彁绀猴紙閬垮厤鏁存 JSON 鐢╁埌椤甸潰锛夈€?+ */ +export function formatApiErrorText(raw: string, status: number): string { +  const text = (raw || "").trim(); +  if (!text) return `璇锋眰澶辫触锛圚TTP ${status}锛塦; +  try { +    const body = JSON.parse(text) as { +      code?: number; +      message?: string; +    }; +    const msg = String(body.message || "").trim(); +    const code = Number(body.code); +    if (code === 40031 || /model not allowed|model disabled|model missing|model not in/i.test(msg)) { +      // 鍏煎鏃ц嫳鏂囨枃妗?+      if (/not allowed for system chat/i.test(msg)) { +        const name = msg.split(":").pop()?.trim() || "褰撳墠妯″瀷"; +        return `妯″瀷銆?{name}銆嶆湭寮€鏀剧郴缁熷璇濓紝璇峰湪杈撳叆鍖哄垏鎹㈠彲鐢ㄦā鍨嬪悗鍐嶅彂閫乣; +      } +      if (/model disabled/i.test(msg)) { +        const name = msg.split(":").pop()?.trim() || "褰撳墠妯″瀷"; +        return `妯″瀷銆?{name}銆嶅凡鍋滅敤锛岃鍒囨崲鍏朵粬鍙敤妯″瀷鍚庡啀鍙戦€乣; +      } +      if (msg) return msg; +    } +    if (msg) return msg; +  } catch { +    /* 闈?JSON */ +  } +  if (text.length > 180) return `璇锋眰澶辫触锛圚TTP ${status}锛塦; +  return text; +} + +/** + * 鍙戣捣 POST SSE 璇锋眰骞堕€愭潯瑙ｆ瀽 event/data锛泂ignal abort 鏃?fetch 鎴?reader.read 鎶涘嚭 AbortError锛屽悜涓婇€忎紶銆?+ */  export async function postSse(    path: string,    body: unknown,    onEvent: SseHandler, +  options?: { signal?: AbortSignal },  ): Promise<void> {    const res = await fetch(path, {      method: "POST", @@ -17,14 +54,14 @@ export async function postSse(      headers: {        "Content-Type": "application/json",        Accept: "text/event-stream", -      "Cache-Control": "no-cache",      },      body: JSON.stringify(body),      cache: "no-store", +    signal: options?.signal,    });    if (!res.ok || !res.body) {      const text = await res.text(); -    throw new Error(text || `HTTP ${res.status}`); +    throw new Error(formatApiErrorText(text, res.status));    }      const reader = res.body.getReader();
```
