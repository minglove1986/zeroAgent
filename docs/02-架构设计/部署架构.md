# zeroAgent 生产部署架构

> 版本 v0.2 | 2026-07-20

---

## 文档版本

| 版本 | 日期 | 主要变更 |
|---|---|---|
| v0.1 | 2026-07-20 | 初版生产部署架构 |
| v0.2 | 2026-07-20 | 同步 v0.2 技术栈（Neo4j 替代 NebulaGraph、移除 Temporal、Langfuse 自托管、OSS 全场景） |

---

## 一、整体部署架构

```
                                ┌────────────────┐
                                │   DNS / CDN    │
                                └────────┬───────┘
                                         │
                                ┌────────▼───────┐
                                │  Nginx / SLB   │
                                │  (负载均衡+WAF)│
                                └────────┬───────┘
                                         │
              ┌──────────────────────────┼──────────────────────────┐
              │                          │                          │
        ┌─────▼─────┐              ┌─────▼─────┐              ┌─────▼─────┐
        │ FastAPI   │              │ FastAPI   │              │ FastAPI   │
        │ Pod 1     │              │ Pod 2     │              │ Pod 3     │
        └─────┬─────┘              └─────┬─────┘              └─────┬─────┘
              │                          │                          │
              └──────────────────────────┼──────────────────────────┘
                                         │
       ┌──────────────────┬──────────────┼──────────────┬──────────────────┐
       │                  │              │              │                  │
  ┌────▼─────┐      ┌─────▼─────┐   ┌────▼────┐   ┌─────▼─────┐    ┌─────▼─────┐
  │  MySQL   │      │   Redis   │   │RabbitMQ │   │  Milvus   │    │   Neo4j   │
  │ (主从)   │      │ (集群)    │   │ (集群)  │   │ (集群)    │    │  Graph    │
  └──────────┘      └───────────┘   └────┬────┘   └───────────┘    └───────────┘
                                          │
                                  ┌───────▼────────┐
                                  │ Celery Worker  │  ← 独立扩缩
                                  │ (3-20 副本)    │
                                  └────────────────┘
                                         │
                                  ┌──────▼─────────┐
                                  │ 阿里云 OSS      │
                                  └────────────────┘

  ┌─────────────────────────────────────────────────────────────┐
  │                      监控层                                  │
  │  ┌─────────┐   ┌─────────┐   ┌─────────┐   ┌─────────┐        │
  │  │Langfuse │   │Prometheus│   │ Grafana │   │  日志    │       │
  │  │ LLM追踪 │   │ 系统指标 │   │  看板   │   │  ELK    │       │
  │  └─────────┘   └─────────┘   └─────────┘   └─────────┘        │
  └─────────────────────────────────────────────────────────────┘
```

---

## 二、K8s 命名空间规划

| Namespace | 用途 |
|---|---|
| `zeroagent-prod` | 生产环境 |
| `zeroagent-staging` | 预发布环境 |
| `zeroagent-dev` | 开发环境 |
| `zeroagent-monitor` | 监控组件（Langfuse / Grafana） |
| `zeroagent-infra` | 中间件（MySQL / Redis / MQ） |

---

## 三、核心 Deployment 配置

### 3.1 FastAPI 主服务

```yaml
# deploy/k8s/api-deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: zeroagent-api
  namespace: zeroagent-prod
  labels:
    app: zeroagent-api
spec:
  replicas: 5
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 2
      maxUnavailable: 1
  selector:
    matchLabels:
      app: zeroagent-api
  template:
    metadata:
      labels:
        app: zeroagent-api
    spec:
      containers:
      - name: api
        image: registry.zeroagent.local/zeroagent-api:v1.0.0
        ports:
        - containerPort: 8000
        env:
        - name: ENV
          value: "production"
        - name: WORKERS
          value: "4"
        resources:
          requests:
            cpu: "500m"
            memory: "1Gi"
          limits:
            cpu: "2000m"
            memory: "4Gi"
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 10
          periodSeconds: 5
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 10
        volumeMounts:
        - name: config
          mountPath: /app/config
      volumes:
      - name: config
        configMap:
          name: zeroagent-config
---
apiVersion: v1
kind: Service
metadata:
  name: zeroagent-api-svc
  namespace: zeroagent-prod
spec:
  selector:
    app: zeroagent-api
  ports:
  - port: 80
    targetPort: 8000
  type: ClusterIP
```

### 3.2 Celery Worker

```yaml
# deploy/k8s/worker-deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: zeroagent-worker
  namespace: zeroagent-prod
spec:
  replicas: 3
  template:
    spec:
      containers:
      - name: worker
        image: registry.zeroagent.local/zeroagent-api:v1.0.0
        command: ["celery", "-A", "app.workers.celery_app", "worker", "--loglevel=info"]
        resources:
          requests:
            cpu: "1000m"
            memory: "2Gi"
          limits:
            cpu: "4000m"
            memory: "8Gi"
```

### 3.3 HPA 自动扩缩容

```yaml
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: zeroagent-api-hpa
  namespace: zeroagent-prod
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: zeroagent-api
  minReplicas: 3
  maxReplicas: 20
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
```

---

## 四、Ingress / 网关

### 4.1 Nginx Ingress

```yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: zeroagent-ingress
  namespace: zeroagent-prod
  annotations:
    nginx.ingress.kubernetes.io/proxy-body-size: "50m"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "300"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "300"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  ingressClassName: nginx
  tls:
  - hosts:
    - api.zeroagent.local
    secretName: zeroagent-tls
  rules:
  - host: api.zeroagent.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: zeroagent-api-svc
            port:
              number: 80
```

### 4.2 Nginx 限流

```nginx
# /etc/nginx/conf.d/zeroagent.conf
limit_req_zone $binary_remote_addr zone=api_limit:10m rate=30r/s;

server {
    listen 80;
    server_name api.zeroagent.local;
    
    location /v1/chat {
        limit_req zone=api_limit burst=50 nodelay;
        proxy_pass http://zeroagent-api-svc;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_read_timeout 300s;
    }
    
    location /v1/workflow/execute {
        # 工作流执行不限流，但单实例限流
        proxy_pass http://zeroagent-api-svc;
        proxy_read_timeout 600s;
    }
}
```

---

## 五、中间件部署

### 5.1 MySQL 主从

```yaml
# 使用 Operator 或 Helm Chart
# 主库：写
# 从库：读（多个）
# 关键配置：
# - innodb_buffer_pool_size = 内存的 70%
# - max_connections = 1000
# - slow_query_log = ON
# - long_query_time = 1
```

**连接配置**：
```
MYSQL_MASTER_URL=mysql://user:pass@mysql-master.zeroagent-infra:3306/zeroagent
MYSQL_SLAVE_URL=mysql://user:pass@mysql-slave.zeroagent-infra:3306/zeroagent
```

### 5.2 Redis 集群

```yaml
# 3 主 3 从
# 用途分配：
# - db 0: 会话缓存
# - db 1: 限流计数
# - db 2: 热点问答缓存
# - db 3: 向量检索缓存
```

### 5.3 RabbitMQ

```yaml
# 3 节点集群
# 队列设计：
# - doc.parse（文档解析）
# - embedding.gen（Embedding 生成）
# - kg.extract（KG 抽取）
# - workflow.exec（工作流执行）
# - notification（IM 通知）
```

### 5.4 Milvus

```yaml
# Helm Chart 部署
# 组件：etcd + minio + pulsar + standalone
# 生产建议：cluster 模式
```

### 5.5 Neo4j（v0.2 替换 NebulaGraph）

```yaml
# Neo4j 社区版（v0.2 调整）
# 单机部署（200-2000 用户规模够用）
# 数据规模：单机千万级实体
# 未来扩展：可平滑迁移到 NebulaGraph 集群
```

---

## 六、配置管理

### 6.1 ConfigMap

```yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: zeroagent-config
  namespace: zeroagent-prod
data:
  config.yaml: |
    app:
      name: zeroagent
      version: 1.0.0
      debug: false
    
    database:
      mysql:
        master_url: "mysql+aiomysql://zeroagent:pass@mysql-master.zeroagent-infra:3306/zeroagent"
        slave_url: "mysql+aiomysql://zeroagent:pass@mysql-slave.zeroagent-infra:3306/zeroagent"
        pool_size: 20
        max_overflow: 40
    
    redis:
      url: "redis://redis.zeroagent-infra:6379/0"
      
    llm:
      providers:
        - name: agnes
          base_url: "https://apihub.agnes-ai.com/v1"
          api_key: "${AGNES_API_KEY}"
          models: ["agnes-2.0-flash"]
        - name: minimax
          base_url: "https://api.minimax.chat/v1"
          api_key: "${MINIMAX_API_KEY}"
          models: ["MiniMax-M3"]
    
    milvus:
      host: "milvus.zeroagent-infra"
      port: 19530
    
    oss:
      endpoint: "oss-cn-hangzhou.aliyuncs.com"
      bucket: "zeroagent-prod"
      access_key: "${OSS_ACCESS_KEY}"
      secret_key: "${OSS_SECRET_KEY}"
```

### 6.2 Secret

```yaml
apiVersion: v1
kind: Secret
metadata:
  name: zeroagent-secrets
  namespace: zeroagent-prod
type: Opaque
stringData:
  AGNES_API_KEY: "sk-xxxx"
  MINIMAX_API_KEY: "sk-xxxx"
  OSS_ACCESS_KEY: "LTAIxxxx"
  OSS_SECRET_KEY: "xxxx"
  MYSQL_PASSWORD: "xxxx"
  REDIS_PASSWORD: "xxxx"
```

---

## 七、CI/CD 流水线

```
Git Push → GitLab CI
   ↓
单元测试 + Lint + 类型检查
   ↓
构建镜像（docker build）
   ↓
推送到镜像仓库（Harbor）
   ↓
自动部署到 staging
   ↓
集成测试
   ↓
手动审批（生产）
   ↓
蓝绿部署到 prod
   ↓
健康检查 + 流量切换
   ↓
旧版本保留 30 分钟（快速回滚）
```

### GitLab CI 示例

```yaml
# .gitlab-ci.yml
stages:
  - test
  - build
  - deploy-staging
  - deploy-prod

test:
  stage: test
  script:
    - pytest tests/ --cov=app --cov-report=xml
    - ruff check app/
    - mypy app/

build:
  stage: build
  script:
    - docker build -t registry.zeroagent.local/zeroagent-api:$CI_COMMIT_SHA .
    - docker push registry.zeroagent.local/zeroagent-api:$CI_COMMIT_SHA

deploy-staging:
  stage: deploy-staging
  script:
    - kubectl set image deployment/zeroagent-api api=registry.zeroagent.local/zeroagent-api:$CI_COMMIT_SHA -n zeroagent-staging
  only:
    - develop

deploy-prod:
  stage: deploy-prod
  script:
    - kubectl set image deployment/zeroagent-api api=registry.zeroagent.local/zeroagent-api:$CI_COMMIT_SHA -n zeroagent-prod
  when: manual
  only:
    - main
```

---

## 八、灾备方案

### 8.1 多可用区部署

```
可用区 A：FastAPI Pod (3) + MySQL 主库 + Redis 主 + RabbitMQ
可用区 B：FastAPI Pod (3) + MySQL 从库 + Redis 从 + RabbitMQ
可用区 C：FastAPI Pod (2) + Milvus 节点 + Neo4j 节点
```

### 8.2 数据备份

| 数据 | 备份策略 |
|---|---|
| **MySQL** | 每日全量 + 每小时 binlog，保留 30 天 |
| **OSS** | 跨区域复制（OSS CRR） |
| **Milvus** | 元数据每日备份，向量数据冗余存储 |
| **Neo4j** | 每日 dump，全量备份 |

### 8.3 故障恢复

| 故障 | RTO | RPO |
|---|---|---|
| 单 Pod 故障 | < 1 分钟 | 0 |
| 节点故障 | < 5 分钟 | 0 |
| 可用区故障 | < 30 分钟 | < 1 分钟 |
| 区域故障 | < 2 小时 | < 5 分钟 |

---

## 九、容量规划

### 初版（500-1000 用户）

| 组件 | 配置 | 数量 |
|---|---|---|
| FastAPI | 2C4G | 3 Pod |
| Celery Worker | 4C8G | 2 Pod |
| MySQL 主 | 4C16G | 1 |
| MySQL 从 | 4C16G | 1 |
| Redis | 2C8G | 1 |
| RabbitMQ | 2C4G | 1 |
| Milvus | 4C16G | 1 |
| Neo4j | 4C16G | 1 |

### 扩展版（5000 用户）

| 组件 | 配置 | 数量 |
|---|---|---|
| FastAPI | 2C4G | 10 Pod |
| Celery Worker | 4C8G | 5 Pod |
| MySQL 主 | 8C32G | 1 |
| MySQL 从 | 8C32G | 2 |
| Redis | 4C16G | 3（集群） |
| RabbitMQ | 4C8G | 3（集群） |
| Milvus | 8C32G | 3（集群） |
| Neo4j | 8C32G | 1（升级单机配置） |