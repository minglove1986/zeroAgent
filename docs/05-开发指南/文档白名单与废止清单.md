# 文档白名单与废止清单（AI 必读）

> 2026-07-21 | 面向 AI Agent 全量开发

## 1. 实现前只读（白名单）

按优先级：

1. `AGENTS.md`
2. `docs/01-产品需求/PRD.md` **第十六章（D1–D42）**
3. `docs/01-产品需求/API接口规范.md`（v0.7.4+）
4. `docs/01-产品需求/数据库表结构.md`（v0.7.4+）
5. `docs/03-技术选型/技术选型.md`（v0.5+）
6. `docs/03-技术选型/MySQL与OSS适配.md`（**仅 v0.3+**）
7. `docs/02-架构设计/总体架构.md`（v0.5.1+）
8. `docs/adr/002-monorepo-web-backend.md`
9. `docs/adr/003-independent-admin-web.md`
10. `docs/05-开发指南/**`
11. `docs/superpowers/plans/**`（当前 Task）
12. `docs/contracts/openapi.yaml`

## 2. 禁止当作现行需求（废止 / 史料）

| 路径 | 原因 |
|---|---|
| PRD **附录 A** | 明示废止史料 |
| `docs/superpowers/specs/2026-07-21-prd-closure-design.md` | 已被第十六章吸收；文首已声明勿实现 |
| `MySQL与OSS适配.md` **v0.2 及更早**段落中的 OpenIM/OSS 事件 | 已用 v0.3 废止 |
| 任何仍写「OpenIM 主入口 / file-callback / IM 告警默认」且未标注废止的旧句 | 以第十六章为准 |

## 3. 硬前提（设计正确性检查表）

实现前自检，任一违反即停：

- [ ] 同仓 monorepo：员工端 `web/`、管理端 `admin-web/`、后端 `src/app`（D34–D35）
- [ ] 管理端仅 `platform_admin` / `super_admin`；生产禁止测试请求头提权（D37）
- [ ] 单租户，无 `tenant_id`
- [ ] 无 OpenIM 客户端、无 `/im/*`、无 `im_user_maps`
- [ ] 对话主入口 Web；SSE + **交互卡片** + `card-action`
- [ ] Agent 提问经技能内 **`ask_user`** → 提问卡（非高风险审批）
- [ ] 文件：Web 上传 → OSS → Celery（非 OSS 事件主路径）
- [ ] Agent 无一等 `tool_ids`；两层 FC
- [ ] LLM 只经 LiteLLM；Langfuse 自托管
- [ ] 高风险仅 `/approvals`
- [ ] 频繁访问的配置/词表：MySQL 真相源 + 启动加载 Redis + 热读缓存 + 管理写后刷缓存（见 `配置类模块缓存约定.md`）

## 4. 与实现计划的关系

按 `docs/superpowers/plans/2026-07-21-zeroagent-mvp.md` **一个 Task 一次**；禁止无计划全量扫 PRD 正文臆造接口。
