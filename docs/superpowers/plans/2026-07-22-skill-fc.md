# 技能层 FC MVP Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 技能层一轮 Function Calling（ask_user / echo / kb_lookup）

**Architecture:** 注册表 → 按 skill_tools 注入 → 非流式 tools 调用 → 执行 / 出卡 → 可选回灌流式

**Tech Stack:** FastAPI、LiteLLM Proxy、pytest、现有 conversation runtime

---

### Task 1: 工具注册表与执行器

**Files:**
- Create: `src/app/modules/tool/registry.py`
- Create: `src/app/modules/tool/executor.py`
- Create: `src/app/modules/tool/__init__.py`
- Create: `src/app/modules/agent/skill_tools.py`
- Test: `tests/test_skill_fc.py`

### Task 2: LLM tools 通路

**Files:**
- Modify: `src/app/modules/llm/client.py`
- Test: Mock 请假 → ask_user tool_call；调用echo → echo

### Task 3: 运行时接入

**Files:**
- Modify: `src/app/modules/conversation/runtime.py`
- Test: Agent+技能 ask_user → card；echo → 回复含结果；系统对话请假仍可用

### Task 4: 断点

- Update: `docs/superpowers/CHECKPOINT.md`
- Run: `pytest -q`
