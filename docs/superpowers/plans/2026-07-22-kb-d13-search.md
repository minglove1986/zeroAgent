# D13 并集鉴权进检索 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 检索时对非管理员按 KbPermission 并集过滤；无授权行拒绝；platform_admin 跳过权限过滤。

**Architecture:** `list_accessible_kb_ids` + 与 `resolve_kb_ids_for_agent` 求交；`run_kb_lookup` 增加用户上下文；runtime/messages 传入 Actor。

**Tech Stack:** 现有 `can_access_kb_union`、`KbPermission`、`UserDepartment`、pytest

## Global Constraints

- 无授权行 = 拒绝；platform_admin 不卡权限  
- Agent 绑定仍生效  
- `@author 赵振明`；东八区时间；无 git 跳过 commit  

---

### Task 1: list_accessible_kb_ids + 单测

**Files:** `permissions.py` 或新建 `access.py`；`tests/test_kb_d13_search.py`

- [x] 实现加载 grants、0 行拒绝、并集通过  
- [x] 单测：无授权 / 有授权命中 / 未命中  

### Task 2: run_kb_lookup 求交 + Actor 接线

**Files:** `lookup.py`；`executor.py`；`runtime.py`；`messages.py`（若需把 actor 传入 stream）

- [x] `run_kb_lookup` 增加 user 上下文与 `is_platform_admin`  
- [x] admin 跳过 accessible 过滤；非 admin 求交  
- [x] 集成测：非 admin 无授权空；admin 可命中  

### Task 3: CHECKPOINT + 全量 pytest

- [x] 更新断点；`pytest -q` 全绿  

---

## Spec：`docs/superpowers/specs/2026-07-22-kb-d13-search-design.md`
