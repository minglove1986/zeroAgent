# zeroAgent PRD 闭环裁定设计

> 日期：2026-07-21  
> **状态：史料（已被 PRD 第十六章吸收）**  
>  
> ⚠️ **AI Agent / 实施：不要以本文为实现依据。**  
> **唯一现行真相**：`docs/01-产品需求/PRD.md` **第十六章（D1–D33）** + 现行 API / 库表 / 技术选型。  
> 本文仅保留早期拍板过程；与第十六章冲突时 **一律以第十六章为准**。

---

## 0. 现行摘要（请去 PRD 看全文）

| 主题 | 现行（v0.7.3） |
|---|---|
| 对话入口 | **Web 系统对话** + 交互卡片 / Agent 提问（D27、D31–D33） |
| OpenIM | **不接入**（D27–D28） |
| 文件入库 | Web 上传 → OSS → Celery（D25） |
| 告警 | 站内通知 + Webhook（D22） |
| 审批 | 仅 Web `/approvals`（D30） |
| 租户 | 单租户（D23） |
| Agent 配置 | 技能+KB/KG+白名单；两层 FC；无 tool_ids（D1–D3） |

---

## 1. 背景（史料）

对 `docs/01-产品需求/PRD.md` v0.6.8 审查后，曾固化 18 条裁定并升至 v0.7.0。其后经 AI 开发导向与 **Web 对话取代 OpenIM** 等修订，裁定编号扩至 **D33**，正文以 PRD 第十六章维护。

## 2. 早期已确认裁定（史料表 · 部分条目已修订）

> 下表为 v0.7.0 时代原文快照。标注「已修订」的以 PRD 第十六章为准。

### 2.1 编排与边界

| ID | 主题 | 史料裁定 | 现行 |
|---|---|---|---|
| D1 | Agent 形态 | 平台内多 Agent；禁外部 A2A | 同左 |
| D2 | 工作流 | 仅技能 workflow_call | 同左 |
| D3 | 配置面 | 技能+KB/KG+白名单 | 同左 |
| D11 | 路由 | IM 澄清卡片 | **Web 澄清卡片** |
| D10 | 测试中 | 不上 IM 路由 | **不进全员 Web 自动路由** |
| D16/D18 | 技能版本 / 取消复合技能 | — | 同左 |

### 2.2 知识库 / 权限 / 安全

| ID | 主题 | 史料裁定 | 现行 |
|---|---|---|---|
| D4/D7/D13/D14 | KB | 专家维护、命中闸门、并集、RAG citation | 同左 |
| D8 | 身份 | OpenIM 自动同步 | **Session = 身份** |
| D9 | 高风险 | IM/Web | **仅 Web** |

### 2.3 配额与工作流

| ID | 主题 | 现行 |
|---|---|---|
| D5 | 用户级+租户硬顶 | 同左（单租户语境下的全局日/月顶） |
| D6 | 实例 DAG 快照 | 同左 |

## 3. Agent 运行时工具面（仍有效）

```
Agent 层：execute_skill / rag_search / call_agent
技能层：原子工具 / workflow_call / **ask_user**（提问卡，D33）
禁止：Agent 层直接 HTTP/DB/文件原子工具
```

## 4. 明确废止的旧表述（累计）

- 外部 A2A；Agent 一等 `tool_ids`；KB 交集；复合技能  
- **OpenIM 主入口 / 同步映射 / file-callback / IM 告警默认**  
- OSS 桶事件作为入库唯一主路径  
- 自助绑工号；多租户实现  

## 5. 后续补裁定索引

| 批次 | ID | 去向 |
|---|---|---|
| AI 开发导向 | D19–D26 | PRD 第十六章（其中 D22/D25 已按 Web 修订） |
| Web 取代 IM | D27–D30 | PRD 第十六章 |
| 交互卡片 | D31–D33 | PRD 第十六章 |

## 6. 验收（史料勾选已由后续文档满足）

现行验收以：`AGENTS.md` + PRD 第十六章 + API/库表 v0.7.3+ + 实现计划 Task DoD。
